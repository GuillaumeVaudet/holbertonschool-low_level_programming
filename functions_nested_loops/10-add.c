#include "main.h"

/**
* add - adds two integers and returns the result
* @a: First parameter to make addition
* @b: Second parameter to make addition
* Return: the result of addition
*/

int add(int a, int b)
{
	return (a + b);
}
